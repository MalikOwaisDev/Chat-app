// Placeholder for rooms routes (Feature 2)
const express = require('express');
const router = express.Router();
const verifyToken = require('../middleware/verifyToken');

// All room routes will be protected — verifyToken applied here
// Routes will be implemented in Feature 2

module.exports = router;
